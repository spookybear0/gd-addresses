AchievementsLayer is where achievements show when clicking on the achievements button in the menu.

Here are the known addresses:

```
AchievementsLayer::init = base + 0x3c2f0
AchievementsLayer::loadPage = base + 0x3c600
```
